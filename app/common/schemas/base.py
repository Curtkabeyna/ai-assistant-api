# Base Pydantic schemas.
# Will contain:
#   - BaseSchema with model_config (from_attributes, populate_by_name)
#   - TimestampMixin schema fields (created_at, updated_at)
#   - IDMixin schema field (id: UUID)
