# AI chat API routes.
# Will contain:
#   - POST /chat/completions — send message, get AI response (sync)
#   - POST /chat/completions/stream — streaming SSE response
#   - POST /conversations/{id}/chat — send message in conversation context
