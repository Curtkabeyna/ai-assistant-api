# Conversations business logic.
# Will contain:
#   - ConversationService.create(), get_by_id(), list_by_user()
#   - ConversationService.update(), delete()
#   - Ownership validation (user can only access own conversations)
