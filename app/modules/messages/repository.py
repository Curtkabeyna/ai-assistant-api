# Messages data access layer.
# Will contain:
#   - MessageRepository class
#   - create(), get_by_id(), list_by_conversation_id()
#   - delete(), bulk_create() for batch inserts
#   - get_conversation_history() ordered by created_at
