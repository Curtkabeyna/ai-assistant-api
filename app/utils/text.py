# Text processing utilities.
# Will contain:
#   - truncate_text(text, max_length) for previews
#   - sanitize_input(text) basic sanitization
#   - count_tokens_estimate(text) rough token count for OpenAI
