# Pagination helper utilities.
# Will contain:
#   - calculate_offset(page, page_size) -> int
#   - calculate_total_pages(total, page_size) -> int
#   - build_paginated_response(items, total, params) -> PaginatedResponse
