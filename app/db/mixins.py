# Reusable SQLAlchemy model mixins.
# Will contain:
#   - UUIDPrimaryKeyMixin (id: UUID primary key)
#   - TimestampMixin (created_at, updated_at with server defaults)
#   - SoftDeleteMixin (deleted_at, is_deleted flag)
